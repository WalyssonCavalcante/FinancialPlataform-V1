version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: meu-banco-postgres
    environment:
      POSTGRES_USER: bank_local
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: financial_platform
    ports:
      - "5432:5432"